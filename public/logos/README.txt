Drop the three company logos here with these exact filenames:

  warner-bros-discovery.png
  outlier.png
  zee.png

PNG or SVG both work (if you use .svg, update the paths in src/data/data.json).
They render inside a white rounded chip, so logos on a white background are
fine — transparency is not required.

Until a file exists, the card falls back to a monogram chip automatically.
